# Shared Dockerfiles

This directory contains shared Dockerfiles for custom images used in the homelab.

## Usage

Build images locally:

```bash
docker build --tag homelab/<image-name> --file <Dockerfile> .
```

## Available Dockerfiles

Currently, all services use official images. Custom Dockerfiles will be added here as needed.
